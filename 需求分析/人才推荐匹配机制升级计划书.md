# 人才推荐匹配机制升级计划书（V2 — 基于合并后代码重新生成）

> **文档定位**：基于合并后最新代码的实际情况，重新制定从"四维度简历匹配"到"五维度能力画像匹配"的升级方案
>
> **重新生成日期**：2026-06-29
>
> **代码勘察日期**：2026-06-29

---

## 一、当前代码现状（实地勘察结果）

### 1.1 现有推荐流程

```
POST /api/enterprise/recommend
  → EnterpriseController.recommend()
    → EnterpriseServiceImpl.recommend()
      ① ProjectParseService.parseProject() → 解析项目描述 → ParseResult(positions+summary)
      ② 遍历所有学生 userMapper.selectList(role=STUDENT)
      ③ 每人调用 CandidateScoreServiceImpl.compute() → CandidateScore
      ④ Top 5 调用 DeepSeek 生成个性化推荐理由
      ⑤ 排序返回 EnterpriseRecommendResponse
```

### 1.2 四维度评分公式

```java
// CandidateScoreServiceImpl.java 第58-61行
综合匹配分 = 技能匹配 × 0.50 + 测评适配 × 0.25 + 学习进度 × 0.15 + 基础匹配 × 0.10
```

| 维度 | 权重 | 当前实现 | 方法 | 代码位置 |
|------|:---:|------|------|---------|
| 技能匹配 | 50% | 解析 CareerProfile.skillTags JSON 与岗位需求比对 | `calcSkillMatch()` | L84-144 |
| 测评适配 | 25% | 取最新一条 AssessmentResult，按岗位类型加权 | `calcAssessmentMatch()` | L186-200 |
| 学习进度 | 15% | `(COMPLETED + IN_PROGRESS×0.5) / total × 100` | `calcLearningProgress()` | L206-213 |
| 基础匹配 | 10% | 学历30%+城市30%+兜底20%+专业20% | `calcBasicMatch()` | L218-271 |

### 1.3 关键类的实际结构

**`CandidateScoreService.CandidateScore`（内部类，定义在接口中）**：
```java
// 现有字段
userId, username, avatar, education, school, major,
matchScore, matchLevel, skillScore, assessmentScore,
learningScore, basicScore, matchedSkills, gapSkills, recommendReason
```

**`EnterpriseRecommendResponse.CandidateItem`**：
```java
// 现有字段（与 CandidateScore 基本对应）
userId, username, avatar, education, school, major,
matchScore, matchLevel, skillScore, assessmentScore,
learningScore, basicScore, matchedSkills, gapSkills, recommendReason
```

**`RecommendConfig`**：
```java
// Scoring 内部类现有字段
weightSkill=0.50, weightAssessment=0.25, weightLearning=0.15, weightBasic=0.10
```

**`application-dev.yml` 配置**：
```yaml
recommend:
  scoring:
    weight-skill: 0.50
    weight-assessment: 0.25
    weight-learning: 0.15
    weight-basic: 0.10
```

### 1.4 已有的数据资产

| 数据源 | 表名 | 关键字段 |
|--------|------|---------|
| 能力测评 | `assessment_result` | programming_score, logic_score, product_score, tech_score, communication_score, total_score |
| 学习路径 | `learning_path` | status, daily_hours, total_days |
| 学习任务 | `learning_task` | status, stage, skill_id, due_date |
| 技能掌握 | `user_skill_mastery` | skill_id, level, source, review_count |
| 个人画像 | `career_profile` | skill_tags(JSON), education, expected_city, school, major |
| 用户表 | `user` | role, education, school, major |

### 1.5 前端现有展示

`RecommendView.vue` 候选人卡片已展示 **4 个维度**的进度条：
- 技能匹配（蓝色）、测评适配（紫色）、学习进度（绿色）、基础匹配（橙色）

---

## 二、升级目标

### 2.1 核心变化

```
四维度 → 五维度：新增"学习成果测评"维度
自报技能权重↓（50%→30%），实测能力权重↑（合计45%）
```

### 2.2 新评分公式

```java
综合匹配分 = 技能匹配 × 0.30 + 能力测试 × 0.25 + 学习进度 × 0.15 + 学习成果测评 × 0.20 + 基础匹配 × 0.10
```

| 维度 | 旧权重 | 新权重 | 变化说明 |
|------|:---:|:---:|------|
| 技能匹配 | 50% | **30%** | 降低自报偏差影响 |
| 能力测试 | 25% | **25%** | 不变，但计算升级（增加趋势） |
| 学习进度 | 15% | **15%** | 不变，但计算升级（阶段加权+时效） |
| 学习成果测评 | — | **20%** | 🆕 新增 |
| 基础匹配 | 10% | **10%** | 不变 |

---

## 三、实施计划（分4阶段）

### P0：数据基础（建表 + 实体 + Mapper）

#### 3.0.1 新增表 `learning_question`（测评题库）

```sql
CREATE TABLE learning_question (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '题目ID',
    stage           VARCHAR(32) NOT NULL COMMENT '阶段：BASIC/FRAMEWORK/PROJECT/INTERVIEW/FINAL',
    skill_id        BIGINT COMMENT '关联技能ID',
    type            VARCHAR(16) DEFAULT 'SELECT' COMMENT '题型：SELECT/MULTI/CODE/ESSAY',
    difficulty      VARCHAR(8) DEFAULT 'MEDIUM' COMMENT 'EASY/MEDIUM/HARD',
    content         TEXT NOT NULL COMMENT '题目内容',
    options         JSON COMMENT '选项JSON（选择题必有）',
    answer          VARCHAR(512) NOT NULL COMMENT '答案',
    score           INT DEFAULT 5 COMMENT '分值',
    knowledge_point VARCHAR(128) COMMENT '考察知识点',
    explanation     TEXT COMMENT '答案解析',
    is_deleted      TINYINT DEFAULT 0,
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_stage_skill (stage, skill_id),
    INDEX idx_stage_diff (stage, difficulty)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='学习成果测评题库';
```

#### 3.0.2 新增表 `learning_result`（测评结果）

```sql
CREATE TABLE learning_result (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    path_id         BIGINT COMMENT '学习路径ID',
    skill_id        BIGINT COMMENT '技能ID',
    type            VARCHAR(32) DEFAULT 'STAGE' COMMENT 'STAGE/FINAL',
    stage           VARCHAR(32) COMMENT 'BASIC/FRAMEWORK/PROJECT/INTERVIEW/FINAL',
    knowledge_score DOUBLE COMMENT '理论得分',
    practice_score  DOUBLE COMMENT '实操得分',
    total_score     DOUBLE COMMENT '总分',
    level           VARCHAR(16) COMMENT '优秀/良好/一般/待提升',
    passed          TINYINT DEFAULT 0 COMMENT '1=通过',
    strengths       TEXT COMMENT '优势总结',
    weaknesses      TEXT COMMENT '薄弱环节',
    ai_analysis     JSON COMMENT 'AI分析',
    ai_suggestions  JSON COMMENT 'AI建议',
    start_time      DATETIME COMMENT '开始时间',
    submit_time     DATETIME COMMENT '提交时间',
    duration_minutes INT COMMENT '耗时',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_path_id (path_id),
    INDEX idx_user_created (user_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='学习成果测评结果表';
```

#### 3.0.3 文件变更

| 操作 | 文件 | 
|------|------|
| 🆕 | `entity/LearningQuestion.java` |
| 🆕 | `entity/LearningResult.java` |
| 🆕 | `mapper/LearningQuestionMapper.java` |
| 🆕 | `mapper/LearningResultMapper.java` |
| 🆕 | `db/migration_v4_learning_result.sql` |

---

### P1：评分引擎升级（改造现有类）

#### 3.1.1 修改 `RecommendConfig.Scoring` — 新增权重字段

```java
// RecommendConfig.java 新增
@Data
public static class Scoring {
    private double weightSkill = 0.30;          // 50→30
    private double weightAssessment = 0.25;
    private double weightLearning = 0.15;
    private double weightLearningResult = 0.20;  // 🆕 新增
    private double weightBasic = 0.10;
}
```

#### 3.1.2 修改 `CandidateScoreService.CandidateScore` — 新增字段

```java
// 新增字段
private double learningResultScore;  // 🆕 学习成果测评得分
```

#### 3.1.3 修改 `CandidateScoreServiceImpl.compute()` — 核心升级

```java
// ① 新增：获取历史测评（不止最新一次）
List<AssessmentResult> history = assessmentResultMapper.selectList(
    new LambdaQueryWrapper<AssessmentResult>()
        .eq(AssessmentResult::getUserId, userId)
        .orderByDesc(AssessmentResult::getCreatedAt).last("LIMIT 5"));
AssessmentResult latest = history.isEmpty() ? null : history.get(0);

// ② 升级：能力测试得分（增加趋势）
double assessmentScore = calcAssessmentMatch(latest, history, positionTitle);

// ③ 升级：学习进度（增加阶段加权+时效）
double learningScore = calcLearningProgress(tasks);

// ④ 🆕 新增：学习成果测评
double learningResultScore = calcLearningResultScore(userId);

// ⑤ 综合加权（5维度）
double total = config.getScoring().getWeightSkill() * skillResult.score
             + config.getScoring().getWeightAssessment() * assessmentScore
             + config.getScoring().getWeightLearning() * learningScore
             + config.getScoring().getWeightLearningResult() * learningResultScore  // 🆕
             + config.getScoring().getWeightBasic() * basicScore;

// ⑥ 设置新字段
result.setLearningResultScore(round(learningResultScore));
```

#### 3.1.4 升级 `calcAssessmentMatch()` — 增加趋势

```java
private double calcAssessmentMatch(AssessmentResult latest, List<AssessmentResult> history, String positionTitle) {
    if (latest == null) return 40.0; // 无测评给低分
    
    // ① 最新成绩（60%）
    Map<String, Double> weights = config.getDimensionWeights(positionTitle);
    double latestScore = nvl(latest.getProgrammingScore()) * weights.getOrDefault("programming", 0.2)
                       + nvl(latest.getLogicScore()) * weights.getOrDefault("logic", 0.2)
                       + nvl(latest.getProductScore()) * weights.getOrDefault("product", 0.2)
                       + nvl(latest.getTechScore()) * weights.getOrDefault("tech", 0.2)
                       + nvl(latest.getCommunicationScore()) * weights.getOrDefault("communication", 0.2);
    
    // ② 趋势（20%）：多次测评趋势
    double trendScore = calcTrendScore(history);
    
    // ③ 等级分（20%）
    double levelScore = levelToScore(getLevel(latest.getTotalScore()));
    
    return latestScore * 0.60 + trendScore * 0.20 + levelScore * 0.20;
}
```

#### 3.1.5 升级 `calcLearningProgress()` — 阶段加权+时效

```java
private double calcLearningProgress(List<LearningTask> tasks) {
    if (tasks == null || tasks.isEmpty()) return 30.0;
    
    LocalDate today = LocalDate.now();
    double completionScore = 0;
    double stageWeightedScore = 0;
    
    Map<String, Double> stageWeights = Map.of(
        "INTERVIEW", 1.5, "PROJECT", 1.3, "FRAMEWORK", 1.0, "BASIC", 0.7
    );
    
    for (LearningTask t : tasks) {
        double stageW = stageWeights.getOrDefault(t.getStage(), 1.0);
        boolean overdue = t.getDueDate() != null && t.getDueDate().isBefore(today);
        
        double contribution;
        if ("COMPLETED".equals(t.getStatus())) contribution = 1.0;
        else if ("IN_PROGRESS".equals(t.getStatus())) contribution = overdue ? 0.25 : 0.50;
        else contribution = overdue ? -0.20 : 0.0;
        
        completionScore += contribution;
        if ("COMPLETED".equals(t.getStatus())) stageWeightedScore += stageW;
        else if ("IN_PROGRESS".equals(t.getStatus())) stageWeightedScore += stageW * 0.5;
    }
    
    double rawScore = (completionScore / tasks.size()) * 50  // 完成度50%
                    + (stageWeightedScore / (tasks.size() * 1.0)) * 30  // 阶段加权30%
                    + 20;  // 时间效率兜底
    
    return Math.min(100, Math.max(10, rawScore));
}
```

#### 3.1.6 🆕 新增 `calcLearningResultScore()`

```java
private double calcLearningResultScore(Long userId) {
    List<LearningResult> results = learningResultMapper.selectList(
        new LambdaQueryWrapper<LearningResult>()
            .eq(LearningResult::getUserId, userId)
            .orderByDesc(LearningResult::getCreatedAt));
    
    if (results == null || results.isEmpty()) return 30.0; // 未测评给低分
    
    // 各阶段加权分
    Map<String, Double> stageWeights = Map.of(
        "FINAL", 1.4, "PROJECT", 1.3, "INTERVIEW", 1.2, "FRAMEWORK", 1.0, "BASIC", 0.8
    );
    double totalWeight = 0, weightedSum = 0;
    for (LearningResult r : results) {
        double w = stageWeights.getOrDefault(r.getStage(), 1.0);
        totalWeight += w;
        weightedSum += w * nvl(r.getTotalScore());
    }
    double stageWeightedScore = totalWeight > 0 ? weightedSum / totalWeight : 50;
    
    // 最新一次成绩
    LearningResult latest = results.get(0);
    double latestScore = nvl(latest.getTotalScore());
    
    // 通过率
    long passed = results.stream().filter(r -> r.getPassed() != null && r.getPassed() == 1).count();
    double passRate = results.size() > 0 ? (double) passed / results.size() * 100 : 0;
    
    return stageWeightedScore * 0.50 + latestScore * 0.30 + passRate * 0.20;
}
```

---

### P2：测评模块（学生端学习成果测评）

#### 3.2.1 API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/student/learning-result/check-ready` | 检查某路径/阶段是否可开始测评 |
| `GET` | `/api/student/learning-result/questions` | 获取测评题目（按阶段+技能随机抽题） |
| `POST` | `/api/student/learning-result/submit` | 提交答案，自动评分 |
| `GET` | `/api/student/learning-result/{id}` | 查看某次测评详情 |
| `GET` | `/api/student/learning-result/history` | 查看测评历史 |

#### 3.2.2 文件变更

| 操作 | 文件 |
|------|------|
| 🆕 | `controller/student/LearningResultController.java` |
| 🆕 | `service/LearningResultService.java` |
| 🆕 | `service/impl/LearningResultServiceImpl.java` |
| 🆕 | `dto/request/SubmitLearningResultRequest.java` |
| 🆕 | `dto/response/LearningResultDetailResponse.java` |

#### 3.2.3 出题规则

- 从 `learning_question` 表按 `stage` + 可选 `skill_id` 随机抽取 10 题
- 题型分布：单选6 + 多选2 + 编程1 + 简答1
- 难度分布：EASY:4, MEDIUM:4, HARD:2
- 选择题：精确匹配答案，全对得分
- 编程/简答题：调用 DeepSeek 对比参考答案评分

### P3：前端展示升级

#### 3.3.1 修改 `RecommendView.vue` 候选人卡片

在现有4维度进度条下方新增第5维度：

```html
<!-- 🆕 新增：学习成果测评 -->
<div class="rmcc-score-row">
  <span class="rmcc-score-label">成果测评</span>
  <div class="rmcc-score-track">
    <div class="rmcc-score-fill" :style="{ width: (cand.learningResultScore || 0) + '%', background: '#ec4899' }"></div>
  </div>
  <span class="rmcc-score-pct">{{ cand.learningResultScore || 0 }}%</span>
</div>
```

#### 3.3.2 新增学生端测评页面

`frontend/src/views/student/LearningResultView.vue`（学习成果测评答题页）

---

### P4：题库建设

为每个阶段准备 20+ 道题目，通过 SQL 文件导入 `learning_question` 表。

| 阶段 | 题量 | 内容覆盖 |
|------|:--:|------|
| BASIC | 20 | Java/Python基础、数据结构、网络基础 |
| FRAMEWORK | 20 | Spring Boot、Vue.js、数据库设计 |
| PROJECT | 20 | 项目架构、代码规范、Git协作 |
| INTERVIEW | 20 | 算法题、系统设计、行为面试 |
| FINAL | 20 | 综合能力、职业素养 |

---

## 四、文件变更总览

| 阶段 | 操作 | 文件 | 预计行数 |
|------|------|------|:--:|
| P0 | 🆕 | `entity/LearningQuestion.java` | ~35 |
| P0 | 🆕 | `entity/LearningResult.java` | ~55 |
| P0 | 🆕 | `mapper/LearningQuestionMapper.java` | ~20 |
| P0 | 🆕 | `mapper/LearningResultMapper.java` | ~20 |
| P0 | 🆕 | `db/migration_v4_learning_result.sql` | ~70 |
| P1 | 🔧 | `config/RecommendConfig.java` | +8 |
| P1 | 🔧 | `service/CandidateScoreService.java` | +5 |
| P1 | 🔧 | `service/impl/CandidateScoreServiceImpl.java` | +100 / -30 |
| P1 | 🔧 | `resources/application-dev.yml` | +2 |
| P2 | 🆕 | `service/LearningResultService.java` | ~25 |
| P2 | 🆕 | `service/impl/LearningResultServiceImpl.java` | ~200 |
| P2 | 🆕 | `controller/student/LearningResultController.java` | ~120 |
| P2 | 🆕 | `dto/request/SubmitLearningResultRequest.java` | ~25 |
| P2 | 🆕 | `dto/response/LearningResultDetailResponse.java` | ~30 |
| P3 | 🔧 | `frontend/src/views/enterprise/RecommendView.vue` | +15 |
| P3 | 🆕 | `frontend/src/views/student/LearningResultView.vue` | ~300 |
| P3 | 🔧 | `frontend/src/api/enterprise.ts` | +2 |
| P3 | 🆕 | `frontend/src/api/learning-result.ts` | ~30 |
| P4 | 🆕 | `db/data/learning_question_init.sql` | ~500 |

---

## 五、不影响现有功能

1. `POST /api/enterprise/recommend` — 接口签名不变，返回结构向后兼容（新增字段）
2. 现有4维度评分逻辑保留但升级算法
3. 前端现有卡片展示不变，仅新增一行
4. 无需修改数据库已有表结构

---

## 六、下一步执行

按 **P0 → P1 → P4 → P2 → P3** 顺序实施：

1. P0（数据基础）：建表 + 实体 + Mapper（0.5h）
2. P1（评分升级）：改造评分引擎（1h）
3. P4（题库建设）：生成初始化数据（0.5h）
4. P2（测评模块）：实现完整测评流程（1.5h）
5. P3（前端展示）：升级推荐卡片 + 新增测评页（1h）

> 总预估：约 4-5 小时（单人），可立即开始实施。
