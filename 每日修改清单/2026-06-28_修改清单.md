# 2026-06-28 项目修改清单（9:00 - 19:00）

> 总计：**30 个文件修改** + **约 20 个新文件** ，净增约 1400+ 行代码

---

## 一、核心功能：「聊一聊」学生-HR 实时聊天系统 ⭐

### 1.1 后端新增文件（全新创建）

| 文件 | 说明 |
|------|------|
| `controller/ChatController.java` | 聊天 REST API（创建会话、发消息、获取消息列表、会话列表、标记已读） |
| `service/ChatService.java` | 聊天服务接口 |
| `service/impl/ChatServiceImpl.java` | 聊天服务实现（含 `createOrGetConversation`、`sendMessage`、`getMessages` 等核心逻辑） |
| `entity/Conversation.java` | 会话实体（JPA 映射） |
| `entity/ChatMessage.java` | 消息实体（JPA 映射） |
| `mapper/ConversationMapper.java` | 会话 MyBatis Mapper |
| `mapper/ChatMessageMapper.java` | 消息 MyBatis Mapper |
| `dto/request/CreateConversationRequest.java` | 创建会话请求 DTO |
| `dto/request/SendMessageRequest.java` | 发送消息请求 DTO |
| `dto/response/ConversationResponse.java` | 会话响应 DTO（含 `studentName`/`hrName`） |
| `dto/response/MessageResponse.java` | 消息响应 DTO（含 `senderRole`/`senderName`） |
| `dto/response/ChatResponse.java` | 聊天统一响应 DTO |

### 1.2 前端新增文件（全新创建）

| 文件 | 说明 |
|------|------|
| `api/chat.ts` | 聊天 API 封装（`createConversation`、`sendMessage`、`getMessages` 等） |
| `stores/chat.ts` | 聊天状态管理（Pinia Store，含会话列表、活跃消息、未读数） |
| `types/chat.ts` | 聊天 TypeScript 类型定义 |
| `views/chat/ChatView.vue` | **聊天页面主界面**（消息列表、输入框、身份标签、已读/未读状态） |

### 1.3 后端修改文件

| 文件 | 修改内容 |
|------|----------|
| `service/impl/ChatServiceImpl.java` | `toConversationResponse()` 增加查询双方真实姓名 |
| `config/WebMvcConfig.java` | 增加 ChatController 拦截器白名单 / CORS 配置 |
| `entity/JobPosition.java` | 岗位实体增加聊天相关字段 |
| `entity/User.java` | 用户实体增加聊天相关字段 |

### 1.4 前端修改文件（接入聊天入口）

| 文件 | 修改内容 |
|------|----------|
| `components/job/JobCard.vue` | 岗位卡片增加「聊一聊」按钮 |
| `components/layout/HeaderBar.vue` | 顶部导航栏增加**消息铃铛**（含未读红点提醒） |
| `router/index.ts` | 新增 `/chat` 路由 |
| `stores/user.ts` | 增加聊天未读数状态 |
| `views/student/JobDetailView.vue` | 岗位详情页增加「聊一聊」入口 |

### 1.5 聊天 UI 细节优化（下午修复）

| 修改点 | 说明 |
|--------|------|
| 消息身份标签 | 每条消息上方显示**蓝色「学生」**或**橙色「HR」**标签 |
| 发送者姓名 | 标签旁边显示对方真实姓名 |
| 自己的消息 | 右侧显示「我 + 角色标签」 |
| 左右对齐 | 对方消息左对齐 + 身份标签在上方，自己消息右对齐 + 身份标签在下方 |

---

## 二、数据库

### 2.1 新建 SQL 脚本

| 文件 | 说明 |
|------|------|
| `mysql-runtime/add_chat_tables.sql` | 聊天表建表脚本 |

### 2.2 新建表（`career_platform` 数据库）

| 表名 | 字段数 | 说明 |
|------|--------|------|
| `conversation` | 14 列 | 聊天会话表（`uk_student_hr_job` 唯一约束，学生+HR+岗位唯一） |
| `chat_message` | 12 列 | 聊天消息表（关联 `conversation_id`） |

### 2.3 修改/新增 SQL

| 文件 | 说明 |
|------|------|
| `backend/src/main/resources/db/init.sql` | 初始化 SQL 增加新表结构 |
| `backend/src/main/resources/db/migration_v2.sql` | 数据库迁移脚本 v2 |
| `backend/src/main/resources/db/migration_v3.sql` | 数据库迁移脚本 v3 |

---

## 三、用户注册登录完善

### 3.1 后端

| 文件 | 修改内容 |
|------|----------|
| `controller/AuthController.java` | 注册/登录逻辑增强，增加密码找回相关接口 |
| `service/impl/AuthServiceImpl.java` | 注册验证逻辑完善（+205 行），邮箱验证、密码强度校验 |
| `service/UserService.java` | 用户服务接口新增方法 |
| `service/impl/UserServiceImpl.java` | 用户服务实现（+19 行） |
| `dto/request/RegisterRequest.java` | 注册请求 DTO 扩展（+40 行，增加邮箱、手机号等字段） |
| `service/MailService.java` | **新文件** - 邮件发送服务 |

### 3.2 前端

| 文件 | 修改内容 |
|------|----------|
| `views/login/LoginView.vue` | 登录页 UI 重做（+172 行），增加密码找回入口 |
| `views/register/RegisterView.vue` | 注册页重构（+269 行），增加邮箱验证、密码强度提示 |
| `views/login/ForgotPasswordView.vue` | **新文件** - 忘记密码页面 |
| `api/auth.ts` | 认证 API 扩展（+35 行） |

---

## 四、企业端功能：岗位发布与管理

### 4.1 后端

| 文件 | 修改内容 |
|------|----------|
| `controller/EnterpriseController.java` | 企业控制器扩展（+60 行） |
| `service/EnterpriseInfoService.java` | **新文件** - 企业信息服务接口 |
| `service/impl/EnterpriseInfoServiceImpl.java` | **新文件** - 企业信息服务实现 |
| `service/JobPublishService.java` | **新文件** - 岗位发布服务接口 |
| `service/impl/JobPublishServiceImpl.java` | **新文件** - 岗位发布服务实现 |
| `mapper/EnterpriseMapper.java` | **新文件** - 企业 Mapper |
| `service/impl/EnterpriseServiceImpl.java` | 企业服务实现微调 |

### 4.2 前端

| 文件 | 修改内容 |
|------|----------|
| `views/enterprise/JobPostView.vue` | 岗位发布页大幅重构（+327 行） |
| `views/enterprise/EnterpriseHome.vue` | 企业首页微调 |
| `api/enterprise.ts` | 企业 API 扩展（+18 行） |

---

## 五、学生端功能：岗位匹配与搜索

| 文件 | 修改内容 |
|------|----------|
| `service/impl/JobMatchingServiceImpl.java` | 岗位匹配逻辑优化 |
| `views/student/JobMatchingView.vue` | 岗位匹配页面优化（+27 行） |
| `utils/jobEnrich.ts` | 岗位数据增强工具 |

---

## 六、基础设施与配置

| 文件 | 修改内容 |
|------|----------|
| `backend/pom.xml` | 增加邮件、文件上传等依赖 |
| `config/WebMvcConfig.java` | CORS / 拦截器白名单更新（+18 行） |
| `util/FileUtil.java` | **新文件** - 文件上传工具类（+77 行） |
| `application-dev.yml` | 增加 DeepSeek API、邮件、文件上传等配置 |
| `vite.config.ts` | Vite 代理配置更新 |
| `api/request.ts` | 请求拦截器微调 |

---

## 七、构建与部署操作

| 操作 | 说明 |
|------|------|
| `mvn clean package` | 重新编译打包后端 JAR |
| MySQL 建表执行 | 通过 `mysql.exe` 直接执行 `conversation` 和 `chat_message` 建表 SQL |
| 重启后端 | 杀掉旧进程 → 启动新 `career-platform-1.0.0.jar`（端口 8080） |

---

## 八、Git 提交记录（今日）

| 时间 | Commit | 说明 |
|------|--------|------|
| 02:05 | `1840624` | chore: 删除实训项目冗余代码目录 |
| 10:26 | `b6a5301` | docs: 添加AI功能实现与完善方向文档 |
| 11:29 | `041695b` | Merge branch 'master' |
